spring:
  datasource:
    url: jdbc:h2:mem:movie-tickets-demo;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:

app:
  seed-demo-data: true

